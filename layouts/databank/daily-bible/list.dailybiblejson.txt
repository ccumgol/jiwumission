{
  {{- $count := 0 -}}
  {{- range $page := .Pages -}}
    {{- if $page.Date -}}
      {{- if $count -}},{{- end -}}
      {{ $page.Date.Format "2006-01-02" | jsonify }}: {
        "title": {{ $page.Title | jsonify }},
        "content": {{ $page.Content | jsonify }}
      }
      {{- $count = add $count 1 -}}
    {{- end -}}
  {{- end -}}
}
